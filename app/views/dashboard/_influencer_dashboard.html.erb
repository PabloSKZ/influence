<br>
<div class="container">
<div class="row justify-content-center">
<div class="col-md-8">
    <div class="card">
          <div class="card-header bg-primary"><h4 class="text-white text-center">Liste de vos projets :</h4></div>
        <div class="card-body">
        <ul class="list-group">
        <% current_user.projects.each do |project| %>
                <li class="list-group-item d-flex justify-content-between align-items-center mb-1 d-flex justify-content-between project-list font-weight-bold">
                    <%= project.title %> <%= link_to "Détails du projet", project_path(project.id) %> <%= link_to "Supprimer ce projet", project_path(project.id), method: :delete  %>  <%= link_to 'Modifier ce projet', edit_project_path(project.id) %>
                </li>
            </ul>
            <% end %>

</div>
</div>
</div>
</div>
</div>
</div>
<br>