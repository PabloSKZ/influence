<h1>Liste de vos projets :</h1>
<div class="container">
    <% current_user.projects.each do |project| %>
    <ul class="list-group">
        <li class="list-group-item d-flex justify-content-between align-items-center mb-1 d-flex justify-content-between">
            <%= project.title %> <%= project.description %> <%= link_to "Détails du projet", project_path(project.id) %> <%= link_to "Supprimer ce projet", project_path(project.id), method: :delete %>
        </li
    </ul>
    <% end %>
</div>