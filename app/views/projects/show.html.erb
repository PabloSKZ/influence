<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class='container' style="margin-top: 5rem;">
    <div class="card mb-5" style="width:80%; margin: auto;">
      <h4 class="card-header bg-primary text-white text-center"><%= @project.title =%></h4>
      <img style="height: auto; width: 100%; display: block;" src="<%= @project.picture =%>" alt="Card image">
      <div class="card-body text-center">
        <p class="card-text"><h5>Description du projet:</h5> <%= @project.description.tr('[\"\"]','') =%></p>
        <P class="card-text"><h6>Liens:</h6> <%= @project.link_field.tr('[\"\"]','') =%></p> 
      <div class="card-body text-muted text-center" >
        <% if current_user != nil && current_user.id == @project.user_id %>
          <%= link_to 'Modifier ce projet', edit_project_path, class: "btn btn-primary" %>
        <% elsif current_user != nil && current_user.user_type == 'freelance' %>
          <% current_user.adverts.each do |advert| %>
            <%= link_to "S\'inviter en tant que #{advert.tag}", project_adverts_projects_join_index_path(advert.id,@project.id), method: :post, class: "btn btn-primary mb-1" %>
          <% end %>
        <% end %>
        <%= link_to 'Retourner à des projets', projects_path, class: 'btn btn-primary' %>
      </div>
    </div>  
</div>
</body>
</html>
